import './stylesheet/verifyotp.css'

export const VerifyOtp = ({ enteredNo, inputOtp }) => {

    return (
        <div>
            <button className="otp_btn">Verify the code</button>
        </div>
    )
}