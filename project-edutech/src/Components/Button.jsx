import React from 'react'
import './Button.css'

export const Button = ({ content }) => {
    return (
        <div>
            <button>{content}</button>

        </div>
    )
}

export default Button
